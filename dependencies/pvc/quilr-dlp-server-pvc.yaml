apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quilr-dlp-server-pvc
  namespace: quilr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 5Gi
