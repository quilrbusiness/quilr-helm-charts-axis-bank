apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.quilr.appname }}-configmap
  namespace: {{ .Values.namespace | default "quilr" }}
data:
  CONFIG_LOADER_PATH: "/{{ .Values.quilr.appname }}"